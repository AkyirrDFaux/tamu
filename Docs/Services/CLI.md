ESP32 Only
Must not interfere with APP Interface!

Allows for interaction (read and write) with all other services except App Interface.

Works over the ESP32-C3's USB Serial/JTAG console (the same port the app interface uses; the two modes hand the port over). No plain UART console is wired up.
